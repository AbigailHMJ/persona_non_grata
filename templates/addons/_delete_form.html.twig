<form method="post" action="{{ path('app_addons_delete', {'id': addon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ addon.id) }}">
    <button class="btn">Delete</button>
</form>
