{% extends 'base.html.twig' %}

{% block title %}Modifier les add-ons{% endblock %}

{% block body %}
{% if app.user %}
    <h1>Modifier les liens externes</h1>

    {{ include('addons/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_addons_index') }}">Revenir en arrière</a>

    {{ include('addons/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
